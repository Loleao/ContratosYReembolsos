@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService AuthorizationService

@{
    string currentController = ViewContext.RouteData.Values["Controller"]?.ToString() ?? "";
    string currentAction = ViewContext.RouteData.Values["Action"]?.ToString() ?? "";

    // Extraemos info de los Claims de la identidad actual (sin ir a la base de datos)
    var fullName = User.FindFirst("FullName")?.Value ?? User.Identity?.Name ?? "Usuario";
    var roleDisplay = User.IsInRole("Admin") ? "Administrador" : "Operador";
}

<aside class="sidebar" id="mainSidebar">
    <div class="sidebar-header mb-4 flex-shrink-0">
        <div class="logo-box">
            <i class="fa-solid fa-shield-halved text-primary"></i>
        </div>
        <div class="sidebar-text ms-2">
            <h6 class="fw-bold mb-0">FONAFUN</h6>
            <small class="text-muted" style="font-size: 10px;">Gestión de Servicios</small>
        </div>
    </div>

    <nav class="nav flex-column flex-grow-1 overflow-y-auto custom-scroll pe-2">
        @* Dashboard: Visible para todos los usuarios autenticados *@
        <a href="@Url.Action("Index", "Home")"
           class="nav-link @(currentController == "Home" ? "active" : "")">
            <i class="fa-solid fa-house-chimney"></i>
            <span class="sidebar-text ms-2">Dashboard</span>
        </a>

        @* Módulo de Contratos *@
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Contratos.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Contract")"
               class="nav-link @(currentController == "Contract" ? "active" : "")">
                <i class="fa-solid fa-file-contract"></i>
                <span class="sidebar-text ms-2">Contratos</span>
            </a>
        }

        @* Módulo de Inventario *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Inventario.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Inventory")"
               class="nav-link @(currentController == "Inventory" ? "active" : "")">
                <i class="fa-solid fa-box"></i>
                <span class="sidebar-text ms-2">Inventario</span>
            </a>
        }

        @* Módulo de Activos Fijos *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Activos.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Asset")"
               class="nav-link @(currentController == "Asset" ? "active" : "")">
                <i class="fa-solid fa-th-large"></i>
                <span class="sidebar-text ms-2">Activos Fijos</span>
            </a>
        }

        @* Módulo de Movilidad *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Movilidad.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Transport")"
               class="nav-link @(currentController == "Transport" ? "active" : "")">
                <i class="fa-solid fa-car-side"></i>
                <span class="sidebar-text ms-2">Movilidad</span>
            </a>
        }

        @* Módulo de Sepulturas *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Cementerios.Ver")).Succeeded)
        {
            <div class="nav-item">
                <a href="@Url.Action("Index", "Cemetery")"
                   class="nav-link @(currentController == "Cemetery" && currentAction != "Templates" ? "active" : "")">
                    <i class="fa-solid fa-monument"></i>
                    <span class="sidebar-text ms-2">Sepulturas</span>
                </a>

                @* Submenú para Modelos Técnicos *@
                @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Modelos.Ver")).Succeeded)
                {
                    <div class="ps-3 mt-1">
                        <a href="@Url.Action("Templates", "Cemetery")"
                           class="nav-link py-1 @(currentAction == "Templates" ? "active text-primary fw-bold" : "text-muted")"
                           style="font-size: 12px; background: transparent !important; box-shadow: none !important;">
                            <i class="fa-solid fa-gears me-2" style="font-size: 10px;"></i>
                            <span class="sidebar-text">Modelos Técnicos</span>
                        </a>
                    </div>
                }
            </div>
        }

        @* Módulo de Filiales *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Filiales.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Branch")"
               class="nav-link @(currentController == "Branch" ? "active" : "")">
                <i class="fa-solid fa-building-user"></i>
                <span class="sidebar-text ms-2">Filiales</span>
            </a>
        }

        @* Módulo de Agencias *@
        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Convenios.Ver")).Succeeded)
        {
            <a href="@Url.Action("Index", "Agency")"
               class="nav-link @(currentController == "Agency" ? "active" : "")">
                <i class="fa-solid fa-handshake"></i>
                <span class="sidebar-text ms-2">Agencias</span>
            </a>
        }

        
        @if ((await AuthorizationService.AuthorizeAsync(User, "Permissions.Usuarios.Gestionar")).Succeeded)
        {
            <a href="@Url.Action("Index", "User")"
               class="nav-link @(currentController == "User" ? "active" : "")">
                <i class="fa-solid fa-users-gear"></i>
                <span class="sidebar-text ms-2">Gestión de Personal</span>
            </a>
        }
    
    </nav>

    @* --- PERFIL DE USUARIO FIJO (FOOTER) --- *@
    @* Usamos 'flex-shrink-0' para que nunca pierda su tamaño *@
    <div class="user-profile border-top pt-3 mt-auto flex-shrink-0">
        <div class="d-flex align-items-center justify-content-between">
            <div class="d-flex align-items-center">
                <div class="avatar bg-light text-primary rounded-circle me-2"
                     style="width: 35px; height: 35px; display: flex; align-items: center; justify-content: center;">
                    <i class="fa-solid fa-user-tie"></i>
                </div>
                <div class="sidebar-text">
                    <p class="mb-0 fw-bold small text-truncate" style="max-width: 100px;">@fullName</p>
                    <small class="text-muted" style="font-size: 10px;">@roleDisplay</small>
                </div>
            </div>

            <form asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
                <button type="submit" class="btn btn-link text-muted p-0 sidebar-text" title="Cerrar Sesión">
                    <i class="fa-solid fa-right-from-bracket"></i>
                </button>
            </form>
        </div>
    </div>
</aside>