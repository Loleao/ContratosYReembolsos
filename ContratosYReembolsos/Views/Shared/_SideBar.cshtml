@{
    // Helpers para las clases active
    string currentController = ViewContext.RouteData.Values["Controller"]?.ToString() ?? "";
    string currentAction = ViewContext.RouteData.Values["Action"]?.ToString() ?? "";
    // Capturamos el parámetro 'type' de la URL para iluminar el sub-menú correcto
    string currentType = Context.Request.Query["type"].ToString().ToUpper();
}

<aside class="sidebar" id="mainSidebar">
    <div class="sidebar-header mb-4">
        <div class="logo-box">
            <i class="fa-solid fa-shield-halved text-primary"></i>
        </div>
        <div class="sidebar-text ms-2">
            <h6 class="fw-bold mb-0">FONAFUN</h6>
            <small class="text-muted" style="font-size: 10px;">Gestión de Servicios</small>
        </div>
    </div>

    <nav class="nav flex-column flex-grow-1">

        <a href="@Url.Action("Index", "Home")"
           class="nav-link @(currentController == "Home" ? "active" : "")">
            <i class="fa-solid fa-house-chimney"></i>
            <span class="sidebar-text ms-2">Dashboard</span>
        </a>


        <a href="@Url.Action("Index", "Contract")"
           class="nav-link @(currentController == "Contract" ? "active" : "")">
            <i class="fa-solid fa-file-contract"></i>
            <span class="sidebar-text ms-2">Contratos</span>
        </a>

        <a href="@Url.Action("Index", "Coffin")"
           class="nav-link @(currentController == "Coffin" ? "active" : "")">
            <i class="fa-solid fa-box"></i>
            <span class="sidebar-text ms-2">Ataúdes</span>
        </a>

        <a href="@Url.Action("Index", "Transport")"
           class="nav-link @(currentController == "Transport" ? "active" : "")">
            <i class="fa-solid fa-car-side"></i>
            <span class="sidebar-text ms-2">Movilidad</span>
        </a>

        <div class="nav-item">
            <div class="d-flex align-items-center justify-content-between @(currentController == "Cemetery" ? "active-container" : "")">
                <a href="@Url.Action("Index", "Cemetery")"
                   class="nav-link flex-grow-1 @(currentController == "Cemetery" ? "active" : "")"
                   style="border-radius: 8px 0 0 8px;">
                    <i class="fa-solid fa-monument"></i>
                    <span class="sidebar-text ms-2">Sepulturas</span>
                </a>

                <a class="nav-link px-3 @(currentController == "Cemetery" ? "active" : "")"
                   data-bs-toggle="collapse"
                   href="#collapseSepulturas"
                   role="button"
                   aria-expanded="@(currentController == "Cemetery" ? "true" : "false")"
                   style="border-radius: 0 8px 8px 0; border-left: 1px solid rgba(255,255,255,0.1);">
                    <i class="fa-solid fa-chevron-down small sidebar-text" style="font-size: 9px;"></i>
                </a>
            </div>

            <div class="collapse @(currentController == "Cemetery" ? "show" : "")" id="collapseSepulturas">
                <ul class="sub-menu list-unstyled">
                    <li>
                        <a href="@Url.Action("Index", "Cemetery", new { type = "PABELLON" })"
                           class="sub-link @(currentType == "PABELLON" ? "fw-bold text-primary" : "")">
                            • Pabellones
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Cemetery", new { type = "COLUMBARIO" })"
                           class="sub-link @(currentType == "COLUMBARIO" ? "fw-bold text-primary" : "")">
                            • Columbarios
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Cemetery", new { type = "TUMBA" })"
                           class="sub-link @(currentType == "TUMBA" ? "fw-bold text-primary" : "")">
                            • Tumbas (Suelo)
                        </a>
                    </li>
                    <li class="mt-2 pt-2 border-top border-secondary opacity-50">
                        <a href="@Url.Action("Templates", "Cemetery")"
                           class="sub-link @(currentAction == "Templates" ? "fw-bold text-primary" : "")">
                            <i class="fa-solid fa-gears me-1" style="font-size: 10px;"></i> Modelos
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="user-profile border-top pt-3 mt-auto">
        <div class="d-flex align-items-center justify-content-between">
            <div class="d-flex align-items-center">
                <div class="avatar bg-light text-primary rounded-circle me-2" style="width: 35px; height: 35px; display: flex; align-items: center; justify-content: center;">
                    <i class="fa-solid fa-user-tie"></i>
                </div>
                <div class="sidebar-text">
                    <p class="mb-0 fw-bold small">Paolo Software</p>
                    <small class="text-muted" style="font-size: 10px;">Administrador</small>
                </div>
            </div>
            <a href="/Account/Logout" class="text-muted sidebar-text" title="Cerrar Sesión">
                <i class="fa-solid fa-right-from-bracket"></i>
            </a>
        </div>
    </div>
</aside>