<div class="add-file-card shadow-lg animate__animated animate__zoomIn">
    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
        <h4 class="fw-bold text-primary mb-0">
            <i class="fa-solid fa-users-viewfinder me-2"></i>Buscador de Afiliados PNP
        </h4>
        <button type="button" class="btn-close" onclick="closeSearchModal()"></button>
    </div>

    <div class="filter-container p-4 mb-4" style="background: #f8fafc; border-radius: 15px; border: 1px solid #e2e8f0;">
        <div class="row g-3">
            <div class="col-md-3">
                <label class="filter-label small fw-bold text-muted">DNI</label>
                <input type="text" id="filterDni" class="form-control rounded-pill" placeholder="8 dígitos" maxlength="8">
            </div>
            <div class="col-md-3">
                <label class="filter-label small fw-bold text-muted">CIP</label>
                <input type="text" id="filterCip" class="form-control rounded-pill" placeholder="N° CIP">
            </div>
            <div class="col-md-4">
                <label class="filter-label small fw-bold text-muted">APELLIDOS / NOMBRES</label>
                <input type="text" id="filterName" class="form-control rounded-pill" placeholder="Buscar texto...">
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="button" class="btn btn-primary w-100 rounded-pill shadow-sm" onclick="executeSearch()">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
            </div>
        </div>
    </div>

    <div class="table-custom-wrapper shadow-sm" style="border-radius: 15px; overflow: hidden; border: 1px solid #eee;">
        <div style="max-height: 300px; overflow-y: auto;">
            <table class="custom-table-modern mb-0">
                <thead style="position: sticky; top: 0; background: #fff; z-index: 10;">
                    <tr>
                        <th class="ps-4">DNI</th>
                        <th>CIP</th>
                        <th>NOMBRE COMPLETO</th>
                        <th class="text-center pe-4">ACCIÓN</th>
                    </tr>
                </thead>
                <tbody id="resultsTableBody">
                    <tr>
                        <td colspan="4" class="text-center py-5 text-muted">
                            <i class="fa-solid fa-info-circle me-2"></i> Ingrese un criterio para buscar
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-4 text-end">
        <button type="button" class="btn btn-light rounded-pill px-4" onclick="closeSearchModal()">Cancelar</button>
    </div>
</div>

<style>
    /* Estilos específicos para que se vea como tu proyecto de Legajos */
    .filter-label {
        display: block;
        margin-bottom: 5px;
        letter-spacing: 0.5px;
    }

    .custom-table-modern {
        width: 100%;
        border-collapse: collapse;
    }

        .custom-table-modern th {
            padding: 15px;
            font-size: 0.8rem;
            text-transform: uppercase;
            color: #64748b;
            border-bottom: 2px solid #f1f5f9;
        }

        .custom-table-modern td {
            padding: 12px 15px;
            font-size: 0.9rem;
            border-bottom: 1px solid #f1f5f9;
        }

        .custom-table-modern tbody tr:hover {
            background-color: #f8fafc;
            transition: 0.2s;
        }

    .add-file-card {
        background: white;
        padding: 30px;
        border-radius: 20px;
    }
</style>