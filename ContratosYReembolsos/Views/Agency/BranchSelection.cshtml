@* @using ContratosYReembolsos.Models.Entities.Branches
@model IEnumerable<Branch>

<div class="container py-5">
    <div class="text-center mb-5">
        <h2 class="fw-bold text-dark">Convenios con Agencias</h2>
        <p class="text-muted">Seleccione la filial para administrar los contratos con funerarias externas</p>
    </div>

    <div class="row g-4 justify-content-center">
        @foreach (var branch in Model)
        {
            <div class="col-md-4">
                <a href="@Url.Action("Index", new { selectedBranchId = branch.Id })"
                   class="card border-0 shadow-sm rounded-4 h-100 text-decoration-none hover-card p-4 text-center">
                    <div class="icon-box bg-success-subtle text-success rounded-circle mx-auto mb-3" style="width: 60px; height: 60px; display: flex; align-items: center; justify-content: center;">
                        <i class="fa-solid fa-handshake fa-2x"></i>
                    </div>
                    <h5 class="fw-bold text-dark mb-1">@branch.Name</h5>
                    <span class="badge bg-light text-success border rounded-pill">
                        @(branch.Agencies?.Count ?? 0) Convenios
                    </span>
                </a>
            </div>
        }
    </div>
</div> *@

@using ContratosYReembolsos.Models.Entities.Branches
@model IEnumerable<IGrouping<string, Branch>>

@{
    ViewData["Title"] = "Selección de Filial - Agencias";
}

<div class="container-fluid py-4 px-md-5 animate__animated animate__fadeIn">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h4 class="fw-bold mb-0 text-dark">Convenios con Agencias</h4>
            <p class="text-muted small">Seleccione una sede para administrar sus convenios con funerarias externas.</p>
        </div>
        <div class="bg-success bg-opacity-10 text-success rounded-pill px-4 py-2 fw-bold border border-success border-opacity-25">
            <i class="fa-solid fa-handshake me-2"></i>Módulo de Convenios
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-4 mb-4 overflow-hidden">
        <div class="card-body p-3 bg-white">
            <div class="row g-3 align-items-center">
                <div class="col-md-7">
                    <div class="input-group bg-light rounded-pill px-3">
                        <span class="input-group-text bg-transparent border-0 pe-0">
                            <i class="fa-solid fa-magnifying-glass text-muted"></i>
                        </span>
                        <input type="text" id="branchSearch" class="form-control bg-transparent border-0 py-2 shadow-none"
                               placeholder="Buscar sede por nombre, código o departamento...">
                    </div>
                </div>
                <div class="col-md-5 text-md-end">
                    <div class="btn-group shadow-none">
                        <button class="btn btn-white border rounded-pill px-3 btn-sm fw-bold me-2" onclick="expandAll()">
                            <i class="fa-solid fa-arrows-expand me-1"></i> Expandir
                        </button>
                        <button class="btn btn-white border rounded-pill px-3 btn-sm fw-bold" onclick="collapseAll()">
                            <i class="fa-solid fa-arrows-collapse me-1"></i> Colapsar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="accordion border-0 shadow-none custom-regions-accordion" id="accordionRegions">
        @foreach (var group in Model.OrderBy(g => g.Key))
        {
            <div class="accordion-item border-0 mb-3 shadow-sm rounded-4 overflow-hidden region-group">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed fw-bold bg-white text-dark py-3 px-4" type="button"
                            data-bs-toggle="collapse" data-bs-target="#collapse-@group.Key.Replace(" ", "")">
                        <div class="d-flex align-items-center w-100">
                            <div class="icon-shape bg-success-subtle text-success rounded-3 me-3">
                                <i class="fa-solid fa-map-location-dot"></i>
                            </div>
                            <span class="fs-6">@group.Key.ToUpper()</span>
                            <span class="badge bg-light text-success border ms-auto me-3 rounded-pill fw-normal">
                                @group.Count() Sedes
                            </span>
                        </div>
                    </button>
                </h2>
                <div id="collapse-@group.Key.Replace(" ", "")" class="accordion-collapse collapse" data-bs-parent="#accordionRegions">
                    <div class="accordion-body p-4 bg-light bg-opacity-50">
                        <div class="row g-3">
                            @foreach (var branch in group.OrderBy(b => b.Name))
                            {
                                <div class="col-xl-4 col-md-6 branch-card-item"
                                     data-search="@branch.Name @branch.Code @branch.Ubigeo.Region @branch.Ubigeo.District">
                                    <div class="card border-0 shadow-sm rounded-4 hover-lift h-100">
                                        <div class="card-body p-3">
                                            <div class="d-flex justify-content-between align-items-start mb-3">
                                                <div class="bg-dark text-white rounded-3 px-2 py-1 fw-bold" style="font-size: 0.65rem;">
                                                    @branch.Code
                                                </div>
                                                <span class="badge @(branch.Agencies?.Count > 0 ? "bg-success" : "bg-secondary") text-white rounded-pill x-small px-2 py-1">
                                                    <i class="fa-solid fa-file-signature me-1"></i> @(branch.Agencies?.Count ?? 0) Agencias
                                                </span>
                                            </div>

                                            <h6 class="fw-bold text-dark mb-1 text-truncate" title="@branch.Name">@branch.Name</h6>
                                            <p class="text-muted mb-3 x-small">
                                                <i class="fa-solid fa-location-dot me-1 text-success opacity-75"></i>
                                                @branch.Ubigeo.District, @branch.Ubigeo.Province
                                            </p>

                                            <div class="d-grid mt-auto">
                                                <a href="@Url.Action("Index", "Agency", new { selectedBranchId = branch.Id })"
                                                   class="btn btn-sm btn-success rounded-pill fw-bold border-0 py-2 shadow-sm">
                                                    Gestionar Agencias <i class="fa-solid fa-arrow-right ms-1"></i>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <div id="emptySearchState" class="text-center py-5 d-none animate__animated animate__fadeIn">
        <div class="bg-light rounded-circle p-4 d-inline-block mb-3">
            <i class="fa-solid fa-magnifying-glass-minus fa-3x text-muted opacity-25"></i>
        </div>
        <h5 class="fw-bold text-dark">No se encontraron resultados</h5>
        <p class="text-muted small">Intente con otro nombre de filial o departamento.</p>
    </div>
</div>

<style>
    .custom-regions-accordion .accordion-button:not(.collapsed) {
        background-color: #fff;
        color: #198754;
        box-shadow: none;
        border-bottom: 1px solid #e9ecef;
    }

    .icon-shape {
        width: 35px;
        height: 35px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .hover-lift {
        transition: all 0.25s ease;
    }

        .hover-lift:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.08) !important;
            border: 1px solid rgba(25, 135, 84, 0.1) !important;
        }

    .x-small {
        font-size: 0.7rem;
    }

    .bg-success-subtle {
        background-color: #e8f5e9 !important;
    }

    .accordion-button:focus {
        box-shadow: none;
        border-color: rgba(0,0,0,.125);
    }
</style>

@section Scripts {
    <script>
        // LÓGICA DE FILTRO (Idéntica a Filiales)
        $('#branchSearch').on('input', function() {
            let term = $(this).val().toLowerCase().trim();
            let totalVisible = 0;

            $('.region-group').each(function() {
                let $group = $(this);
                let hasMatchInGroup = false;

                $group.find('.branch-card-item').each(function() {
                    let searchData = $(this).data('search').toLowerCase();
                    if (searchData.includes(term)) {
                        $(this).show();
                        hasMatchInGroup = true;
                        totalVisible++;
                    } else {
                        $(this).hide();
                    }
                });

                if (hasMatchInGroup) {
                    $group.show();
                    if (term.length > 2) {
                        $group.find('.accordion-collapse').addClass('show');
                        $group.find('.accordion-button').removeClass('collapsed');
                    }
                } else {
                    $group.hide();
                }
            });

            if (totalVisible === 0) $('#emptySearchState').removeClass('d-none');
            else $('#emptySearchState').addClass('d-none');
        });

        function expandAll() {
            $('.accordion-collapse').addClass('show');
            $('.accordion-button').removeClass('collapsed');
        }

        function collapseAll() {
            $('.accordion-collapse').removeClass('show');
            $('.accordion-button').addClass('collapsed');
        }
    </script>
}